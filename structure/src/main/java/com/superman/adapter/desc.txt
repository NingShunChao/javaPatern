适配器模式

描述：
      将一个类的接口转换成客户期望的另外一个接口，使得原本由于接口不兼容而不能一起工作的
      哪些类可以一起工作。本质是转换匹配，复用功能

优点：
    更好的复用性，更好的扩展性

缺点：
    过多使用会使系统混乱，不容易进行整体把控

使用场景：
    1.已存在的类不符合客户端调用需求，可以使用适配器对象转换成需要的接口
    2.需创建一个可复用的类，这个类可能和不兼容的类一起工作，可以使用适配器模式以
    期需要什么就适配什么
